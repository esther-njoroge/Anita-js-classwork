let a = 'Hello';
let b = "World";
let c = `There`;

let length = a.length;
console.log({ length});
console.log('item', a[2]);
// console.log( 'index', a.indexOf( 'e'));
// length of a string( above)

// string concatenation ( +)
let greet = a +" " +b;
console.log({ greet});

// template literals
let salamu = `${a} ${b}. I love code!`;
console.log({ salamu});

console.log(a.indexOf('l'))








function occurrence (word){
    var m = word.split()
    if (m[i] === m[i]{
        y += m[i]
        console.log(y.count())
    })
occurrence("apple")
} 